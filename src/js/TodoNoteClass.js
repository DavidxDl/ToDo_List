export default class Note {
    constructor(title, info, priority, time){
        this.title = title;
        this.info = info;
        this.priority = priority;
        this.time = time;
    }

}